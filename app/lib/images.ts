/**
 * Picsum placeholder image URL.
 * Swap for real photography (and `next/image`) once production assets land.
 */
export const img = (seed: string, width: number, height: number) =>
  `https://picsum.photos/seed/${seed}/${width}/${height}`;
